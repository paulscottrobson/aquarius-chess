; ***************************************************************************************
; ***************************************************************************************
;
;		Name : 		memory.def
;		Author :	Paul Robson (paul@robsons.org.uk)
;		Date : 		14th October 2021
;		Purpose :	Memory operators
;
; ***************************************************************************************
; ***************************************************************************************

@copies	!
		ld 		(hl),e
		inc 	hl
		ld 		(hl),d
		dec 	hl
@end

; ***************************************************************************************

@copies	@
		ld 		a,(hl)
		inc 	hl
		ld		h,(hl)
		ld		l,a
@end

; ***************************************************************************************

@calls	+!
		ld 		a,(hl)
		add 	a,e
		ld 		(hl),a
		inc 	hl
		ld 		a,(hl)
		adc 	a,d
		ld 		(hl),a
		dec 	hl
		ret
@end

; ***************************************************************************************

@copies	c!
		ld 		(hl),e
@end

; ***************************************************************************************

@copies	c@
		ld 		l,(hl)
		ld 		h,0
@end

; ***************************************************************************************

@calls	p@
		push 	bc
		ld		b,h
		ld 		c,l
		in 		l,(c)
		ld 		h,0
		pop 	bc
		ret
@end

; ***************************************************************************************

@calls 	p!
		push 	bc
		push 	hl
		ld 		a,e
		ld		b,h
		ld 		c,l
		out 	(c),a
		pop 	hl
		pop 	bc
		ret
@end
	
	